---
title: "SupercalifragilisticexpialidociousAntidisestablishmentarianismPneumonoultramicroscopic<script>alert(1)</script>\"q\"&amp;🚀"
description: "A title engineered to break layout, escaping, and the reader's patience — 120 characters of hostility, with a script tag, quotes, an entity, an emoji, and no mercy."
pubDate: 2026-08-01
tags: ["adversarial"]
draft: true
---

A post whose title contains a `<script>` tag, `"double quotes"`, an `&amp;` entity, an emoji 🚀, and 120 characters without a single space in the first half. The renderer must escape all of it and wrap the unbreakable run.

نص عربي للاختبار — RTL text inline, to check bidi handling in the prose.

## A heading with <em>markup</em> and `code`

Body copy follows, unremarkable on purpose.
