---
interface Props {
	rotate?: number;
	color?: string;
	class?: string;
}

const { rotate = -3, color = "#b84a1f", class: className } = Astro.props;
---

<span
	class:list={["marginalia", className]}
	style={`transform: rotate(${rotate}deg); color: ${color}`}
>
	<slot />
</span>
